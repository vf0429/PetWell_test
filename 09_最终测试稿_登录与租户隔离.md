# PetWell 商家端：最终测试稿（登录鉴权 + 租户隔离）

## 1) 测试账号（已由后端 seed 自动写入数据库）

### A. 商品商家管理员（Shop）
- Email: `shop_admin@petwell.com`
- Phone: `+85290000011`
- Password: `Shop123456`
- tenant_id: `tenant_shop_demo`
- merchant_type: `shop`

### B. 医院商家管理员（Clinic）
- Email: `clinic_admin@petwell.com`
- Phone: `+85290000022`
- Password: `Clinic123456`
- tenant_id: `tenant_clinic_demo`
- merchant_type: `clinic`

### C. Google 测试账号（演示）
- Email: `google_shop@petwell.com`
- 当前为 demo 模式（不走真实 OAuth 授权页）
- tenant_id: `tenant_shop_demo`

---

## 2) 鉴权与隔离机制（当前实现）

- 登录接口：`POST /api/merchant/auth/login`
- 成功后返回 `session_id`
- 后续所有业务接口要求请求头：`X-Session-ID: <session_id>`
- 服务端根据 session 获取 tenant，再做数据过滤和写入归属
- 客户端传入的 `tenant_id` 会被服务端覆盖，防止越权写入

---

## 3) 核心测试用例

## TC-01 邮箱登录成功（Shop）
- 步骤：用 `shop_admin@petwell.com / Shop123456` 登录
- 预期：返回 `200`，并有 `session_id` 与 `user.tenant_id=tenant_shop_demo`

## TC-02 手机号登录成功（Clinic）
- 步骤：`country_code=+852`, `phone=90000022`, `password=Clinic123456`
- 预期：返回 `200`，并有 `user.tenant_id=tenant_clinic_demo`

## TC-03 密码错误登录失败
- 步骤：任一账号使用错误密码登录
- 预期：返回 `401`，错误 `invalid credentials`

## TC-04 未带 Session 访问业务接口
- 步骤：不传 `X-Session-ID` 调用 `/api/merchant/orders`
- 预期：返回 `401`，错误 `missing session`

## TC-05 Shop 账户看不到 Clinic 数据
- 步骤：Shop 登录后调用 `/api/merchant/appointments` `/api/merchant/visits`
- 预期：只返回 `tenant_shop_demo` 的记录（通常为空或仅本租户数据）

## TC-06 Clinic 账户看不到 Shop 数据
- 步骤：Clinic 登录后调用 `/api/merchant/orders` `/api/merchant/products`
- 预期：不出现 `tenant_shop_demo` 的记录

## TC-07 强制写入隔离（防伪造 tenant_id）
- 步骤：Shop 登录后创建订单时 body 里手工写 `tenant_id=tenant_clinic_demo`
- 预期：最终落库仍是 `tenant_shop_demo`

## TC-08 处方越权防护
- 步骤：Shop 账号尝试对 Clinic 的 `visit_id` 创建处方
- 预期：返回 `403`，错误 `visit does not belong to current tenant`

## TC-09 Google 登录（Demo）
- 步骤：点击前端 Google 登录
- 预期：返回 Shop demo 账户 session，可进入系统并访问 `tenant_shop_demo` 数据

---

## 4) API 快速示例

```bash
# 1) 登录（邮箱）
curl -X POST http://localhost:8090/api/merchant/auth/login \
  -H "Content-Type: application/json" \
  -d '{"method":"email","email":"shop_admin@petwell.com","password":"Shop123456"}'

# 2) 使用 session 访问订单
curl http://localhost:8090/api/merchant/orders \
  -H "X-Session-ID: <session_id>"
```

---

## 5) Google Login 目前如何实现

当前是 **Demo 实现**：
- 前端点击 Google 登录时，调用同一个登录接口，method=`google`
- 后端在 `staff_users` 中查 `auth_provider='google'` 的测试用户（或指定 email）
- 返回 session 并进入系统

这不是正式 OAuth。正式版需要：
1. 前端跳转 Google OAuth 授权页  
2. 后端处理 callback（code -> token）  
3. 验证 Google ID Token（iss/aud/exp/sub）  
4. 建立或绑定本地用户  
5. 创建 session 并下发
