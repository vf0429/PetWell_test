# PetWell 商家端：总览、Shopify 接入与测试运行说明（V2）

## 1. 当前版本状态（截至本次更新）

## 前端（Web）
- 登录页支持：邮箱 + 密码、手机号 + 密码、Google（Demo）
- 中英切换：登录页与主界面都支持
- 登录后支持新手 Guide（右下角 Next）
- 业务页：Dashboard / Orders / Catalog / Customers / Analytics / Settings

## 后端（Go）
- 默认已接入 SQLite 持久化（`backend/db/petwell_merchant.db`）
- 登录鉴权已接数据库（`staff_users` + `sessions`）
- 所有业务 API 强制 `X-Session-ID`
- 租户隔离已开启：按 session 的 `tenant_id` 读写与过滤
- 关键防护：
  - 客户端伪造 `tenant_id` 会被服务端覆盖
  - 处方/回访会校验 `visit` 是否属于当前租户

## 测试账号（seed 自动生成）
- Shop Admin：`shop_admin@petwell.com / Shop123456`（tenant: `tenant_shop_demo`）
- Clinic Admin：`clinic_admin@petwell.com / Clinic123456`（tenant: `tenant_clinic_demo`）
- Google Demo：`google_shop@petwell.com`（demo 登录，不走真实 OAuth）

---

## 2. Shopify 接入：推荐方法与准备工作

## 2.1 目标原则（先定策略）
- **商品/订单/库存主数据以 Shopify 为准**
- PetWell 商家端做运营增强、医疗联动、分析与自动化
- 首期建议 **只读同步 + 状态同步**，稳定后再开写入

## 2.2 接入前准备（Checklist）
1. Shopify Partner 账号
2. 创建 App（建议 OAuth）
3. 获取 `API Key / API Secret`
4. 配置 Redirect URL（PetWell 后端回调地址）
5. 配置 Webhook 回调 URL（PetWell 后端）
6. 准备运行环境变量与密钥管理（不要硬编码）
7. 准备公网可访问域名（本地联调可用 ngrok）

## 2.3 建议 Scopes（最小权限起步）
- 只读首期：`read_products`, `read_inventory`, `read_orders`, `read_customers`, `read_fulfillments`
- 需要反向操作再加：`write_inventory`, `write_orders`, `write_fulfillments`

## 2.4 首期必须接的 Webhooks
- `orders/create`
- `orders/paid`
- `orders/cancelled`
- `fulfillments/create`
- `fulfillments/update`
- `inventory_levels/update`

## 2.5 后端实现步骤（建议执行顺序）
1. OAuth：安装授权 -> callback 换 token -> 按店铺安全存储 token  
2. Webhook 验签：校验 HMAC（失败返回 401/403）  
3. 数据标准化：Shopify 字段映射到 PetWell 统一模型  
4. 事件处理：入库 + 幂等（同一 webhook_id 不重复处理）  
5. 异常策略：重试队列 + 告警 + 对账任务

## 2.6 生产建议
- 密钥放 Secret Manager
- 外部请求统一超时 + 重试 + 限流
- 关键动作写审计日志
- 必做监控：成功率、延迟、队列堆积、验签失败次数

---

## 3. 测试文档怎么用（你现在该看哪份）

## 3.1 `05_阶段性测试案例.md`
- 用途：阶段性全景测试矩阵（产品评审、联调排期）
- 适合：团队讨论与里程碑验收

## 3.2 `09_最终测试稿_登录与租户隔离.md`
- 用途：当前版本可直接执行的核心测试（登录、鉴权、租户隔离）
- 适合：你现在做真机联调与回归

## 3.3 `06_API冒烟测试脚本.sh`
- 用途：快速 API 验证脚本（自动登录 + 一组 CRUD + 列表检查）
- 适合：每次改后端后快速 smoke test

---

## 4. 如何运行当前系统与测试

## 4.1 启动后端
```bash
cd backend
go run .
```

可选环境变量：
- `PETWELL_STORE=sqlite`（默认）
- `PETWELL_STORE=memory`（回退内存）
- `PETWELL_DB_PATH=./db/petwell_merchant.db`（自定义数据库路径）

## 4.2 打开前端
- 打开 `web/index.html`（或用本地静态服务器）
- 使用测试账号登录（见上面账号）

## 4.3 运行 API 冒烟测试
```bash
cd /Users/vfzzz/Desktop/Petwell商家端
bash 06_API冒烟测试脚本.sh
```

可选：
```bash
BASE_URL=http://localhost:8090 bash 06_API冒烟测试脚本.sh
```

脚本会做：
- 登录并拿到 `session_id`
- 依次创建客户/商品/订单/预约/就诊/处方/回访
- 拉取 dashboard 与列表接口验证结果

---

## 5. Google Login 现状说明（非常重要）

- **当前是 Demo 逻辑，不是正式 Google OAuth**
- 现在实现：前端发 `method=google`，后端在 `staff_users` 中查 `auth_provider='google'` 用户后发 session
- 用于联调和流程演示，不满足正式安全要求

正式版应改为：
1. 前端跳转 Google 授权页  
2. 后端 callback 兑换 token  
3. 验证 ID Token（iss/aud/exp/sub）  
4. 绑定/创建本地用户  
5. 创建 session 并下发

---

## 6. 下一阶段优先级建议（从当前到可上线）
1. 完成真实 Shopify OAuth + Webhook 验签 + 幂等  
2. 完成真实 Google OAuth（替换 demo）  
3. 增加 logout 与 session 撤销  
4. 增加自动化回归脚本（CI 执行 smoke + 隔离测试）  
5. 增加错误监控与审计日志（上线必需）
