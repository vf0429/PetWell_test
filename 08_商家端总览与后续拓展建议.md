# PetWell 商家端：总览与后续拓展建议（V1）

## 1. 当前已完成（本阶段）

## 前端（Web）
- 已完成商家端多页面原型与功能交互：
  - Dashboard
  - Orders / Appointments
  - Products / Visits
  - Customers / Prescriptions
  - Settings（Shopify 占位测试）
- 支持双模式切换：`商品商家` / `医院商家`
- 表单提交后可实时刷新对应列表（已接后端 API）

## 后端（Go）
- 已实现 API（内存存储版，便于联调）：
  - Shop: orders / products / customers
  - Clinic: appointments / visits / prescriptions / followups
  - dashboard 汇总
  - Shopify 占位：oauth start / webhook accept
- 已有 demo seed 数据
- 已有数据库 schema 草案（`backend/db/schema.sql`）

## 测试与文档
- 已有实施 checklist（已勾选）
- 已有阶段性测试案例
- 已有 API 冒烟脚本
- 已有 App 联调说明（字段映射、时序、幂等）

---

## 2. 当前目录关键文件
- `web/index.html`：商家端页面结构
- `web/app.js`：前端 API 调用与页面逻辑
- `backend/internal/server.go`：路由与接口
- `backend/internal/store.go`：当前存储逻辑（内存）
- `backend/db/schema.sql`：数据库表草案
- `06_API冒烟测试脚本.sh`：接口快速验证

---

## 3. 商家端 API 如何接入与维护（重点）

## 3.1 Shopify 接入（建议正式方案）
1. 在 Shopify Partner 创建 App
2. 实现 OAuth 授权回调（按店铺保存 token）
3. 订阅 Webhooks（orders/create、orders/paid、fulfillments/update、inventory_levels/update）
4. 后端验签并入库
5. 异步任务做重试与补偿

## 3.2 需要的 Shopify 权限（最小化）
- `read_products`, `read_inventory`, `read_orders`, `read_customers`
- 如需反向写入再增加：`write_inventory`, `write_orders`, `write_fulfillments`

## 3.3 生产维护建议
- Token 与密钥统一放到环境变量/密钥服务
- Webhook 验签失败要告警
- 所有外部调用加超时、重试、限流
- 关键动作落审计日志（订单状态改动、处方改动）
- 指标监控：接口成功率、延迟、Webhook 堆积长度

---

## 4. 医院端后端下一步（从“可演示”到“可商用”）
1. 存储层由内存改为数据库（Postgres/SQLite）
2. 增加身份认证与 RBAC（管理员/医生/店员）
3. 引入租户隔离中间件（tenant_id 强校验）
4. 就诊状态机约束（避免非法流转）
5. 文件上传改对象存储（S3/OSS）+ 签名 URL
6. 增加同步任务队列（向 App 推送摘要）

---

## 5. 后续拓展路线图（建议）

## Phase A（1-2 周）
- 接入真实数据库
- 完成登录鉴权
- 把当前前端 CRUD 全部联到真实持久化

## Phase B（2-4 周）
- Shopify OAuth + Webhook 真接入
- 医院端文件上传与回访提醒
- 与 App 的订单/病历摘要双向同步

## Phase C（4-8 周）
- 数据分析报表完善
- 自动化运营（低库存预警、复诊推荐）
- 保险/商品推荐策略与宠物档案联动

---

## 6. 仍需你确认的关键决策
1. 先主推哪类商家：商品还是医院？
2. 数据库最终选型：先 SQLite 还是直接 Postgres？
3. App 同步是实时推送还是分钟级批量？
4. Shopify 首期是否只读（更稳）还是直接可写（更快）？

---

## 7. 交付状态结论
- 当前版本已可用于：
  - 方案评审
  - 前后端联调演示
  - API 讨论与数据模型对齐
- 尚未完成的“生产级能力”：
  - 鉴权、持久化、审计/监控、真实 Shopify OAuth 与验签

