# PetWell 商家端文档导航（README）

> 目的：让团队和 GitHub 浏览者快速知道每个文档的用途、当前状态、从哪里开始看。

## 建议阅读顺序（第一次进入项目）
1. `08_商家端总览与后续拓展建议.md`（全局总览）
2. `09_最终测试稿_登录与租户隔离.md`（当前可执行测试）
3. `06_API冒烟测试脚本.sh`（一键接口冒烟）
4. `03_Shopify对接清单.md`（Shopify 接入方法）
5. `07_App联调说明.md`（与 App 的同步策略）

---

## 文档索引（用途一览）

| 文件 | 用途 | 当前是否可直接执行 |
|---|---|---|
| `00_实施Checklist.md` | 项目实施进度与完成项清单 | 是（勾选式） |
| `01_信息架构与页面地图.md` | 业务信息架构、角色、页面地图 | 是 |
| `02_商家端线框稿.html` | 早期线框稿（结构参考） | 是 |
| `03_Shopify对接清单.md` | Shopify 接入准备、scope、webhook、实施步骤 | 是 |
| `04_医院端后端数据模型与API草案.md` | 医院侧数据模型与 API 草案（设计文档） | 是（草案） |
| `05_阶段性测试案例.md` | 阶段测试矩阵（里程碑级） | 是 |
| `06_API冒烟测试脚本.sh` | 自动化冒烟测试脚本（登录+核心接口） | 是 |
| `07_App联调说明.md` | 商家端与 App 的字段映射与同步时序 | 是 |
| `08_商家端总览与后续拓展建议.md` | 当前版本总览 + Shopify 方法 + 测试运行说明 | 是 |
| `09_最终测试稿_登录与租户隔离.md` | 当前版本核心测试账号与执行用例 | 是 |

---

## 当前实现状态（2026-03）
- 后端已从纯内存升级为默认 SQLite 持久化
- 登录改为数据库验证（邮箱/手机号+密码，Google Demo）
- 会话机制启用：业务 API 需 `X-Session-ID`
- 租户隔离启用：按 session 的 `tenant_id` 强制过滤和写入
- 前端支持中英切换、登录、Guide 引导

---

## 快速运行

```bash
# 1) 启动后端
cd backend
go run .
```

```bash
# 2) 运行冒烟测试（在项目根目录）
bash 06_API冒烟测试脚本.sh
```

```bash
# 3) 切换为医院测试账号执行
LOGIN_EMAIL=clinic_admin@petwell.com LOGIN_PASSWORD=Clinic123456 bash 06_API冒烟测试脚本.sh
```

---

## 说明
- Google 登录目前是 Demo（非正式 OAuth），详见：`09_最终测试稿_登录与租户隔离.md`
- 正式上线前优先补齐：真实 Google OAuth、真实 Shopify OAuth/Webhook 验签、审计与监控
